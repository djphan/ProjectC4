var loginApp = angular.module('loginApp', []);

/*
newsApp.controller('newsController', function($scope, $http) {
  $scope.isBusy = true;

  // Clear articles
  $scope.user = []

  $http.get("data/user")
  .then(function(result) {
    // Success
    angular.copy(result.data, $scope.articles);
  }, function() {
    // Error
    // TODO: Improve error handling
    alert("Could not grab User");
  }).then(function() {
    // Initialize Skrollr
    skrollr.init({
      forceHeight: false
    });
    $scope.isBusy = false;
  });
});
*/
